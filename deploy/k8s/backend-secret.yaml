apiVersion: v1
kind: Secret
metadata:
  name: kuberoot-backend-secret
  namespace: kuberoot
type: Opaque
stringData:
  # Must match postgres-secret.yaml POSTGRES_PASSWORD
  DATABASE_URL: postgres://kuberoot:replace-with-secure-password-32-chars-min@kuberoot-postgres:5432/kuberoot?sslmode=disable
